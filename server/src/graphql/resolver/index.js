const CategoryResolver = require('./category.resolver')
const FormationResolver = require('./formation.resolver')
const LanguageResolver = require('./language.resolver')
const StatusResolver = require('./status.resolver')
const TrainerResolver = require('./trainer.resolver')

module.exports = {
  CategoryResolver,
  StatusResolver,
  LanguageResolver,
  TrainerResolver,
  FormationResolver,
}